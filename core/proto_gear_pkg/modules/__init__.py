"""Departmental modules.

Each subdirectory with a ``module.yaml`` is a departmental module (see
:mod:`proto_gear_pkg.module_manifest`). ``engineering`` is the reference
implementation; new departments are added by dropping a new directory here —
no core edits (ADR-001 Phase B exit criterion).
"""
