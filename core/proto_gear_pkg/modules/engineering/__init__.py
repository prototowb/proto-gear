"""Engineering department module — the reference module implementation.

Module #1 of the agency OS (PROJECT_SPECIFICATIONS.md): encodes tickets,
branching, testing, releases, and code review as capabilities + templates.

The engineering module's engine code (templates, detection, wizards, ticket
state) currently lives at the package root and is migrating here across
ADR-001 Phase B. The contract surfaces it manages are declared in
``module.yaml``.
"""
