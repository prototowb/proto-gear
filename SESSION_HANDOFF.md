# SESSION_HANDOFF — proto-gear

> **Read this before anything else.** This file captures what's true right now.
> **At session end**: replace the contents entirely — do not append.
> This is a rolling "current state" snapshot, not a log.

## What Just Shipped

- **SESSION_HANDOFF.md** — new living handoff document; seeded on `pg init`, agent-maintained
- **AGENTS.template.md** — SESSION_HANDOFF.md added to links, mandatory reading (item 1, READ FIRST), pre-flight checklist, and self-config table; existing items renumbered 2–10
- **BRANCHING.template.md** — replaced with oomph-based content; new placeholders: `EXAMPLE_POST_MERGE`, `BEFORE_MERGE_STEPS`, `QUICK_REMOTE_RULES`, `QUICK_NEVER_REMOTE`, `PUSH_DEV_INFO`; remote case now shows "merge to dev locally → push dev → PR dev→main"
- **sync_context.py** — SESSION_HANDOFF.md added to CORE_FILES (before PROJECT_STATUS.md)
- **interactive_wizard.py** — SESSION_HANDOFF excluded from optional templates pool (it's always core)
- **proto_gear.py** — SESSION_HANDOFF in proto_gear_files list; seeded on init (skip if exists, never clobber); generate_branching_doc updated with new placeholders

## Pending / In Progress

*Nothing pending — session complete.*

## Conventions In Force

- SESSION_HANDOFF.md is **agent-owned** — never regenerated by `pg sync-context` or `pg doctor --fix`
- Not in `CORE_DOC_FILES` (doctor doesn't check its header — it's a living doc, not a template)
- On `pg init`, seeded only if absent; uses `current_dir.resolve().name` for project name (not `.name` which returns empty for `Path(".")`)

## Open Questions

*None.*

---
*Agent-maintained. Replace entirely at session end.*
